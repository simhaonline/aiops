interface:
  display_name: "Aiops"
  short_description: "Repo-specific patterns and workflows for aiops"
  default_prompt: "Use the aiops repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true